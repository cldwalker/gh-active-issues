<!DOCTYPE html>
<head>
<title>{{github-user}}'s Github Waiting Room</title>
<link href="/application.css" rel="stylesheet" type="text/css" />
</head>
<body>
<h1>{{github-user}}'s Github Waiting Room</h1>
<ol>
{{#issues}}
    <li id="issue_{{position}}">
      <span>{{position}}.</span>
      <p class="name">
        <a href="{{url}}">{{name}}</a>
        created by <a href="https://github.com/{{user}}">{{user}}</a>
        on {{created}} (<a id="{{name}}" href="#{{name}}">permalink</a>)
      </p>
      <p class='title'>
        <a href="{{url}}">{{title}}</a>
        {{type}} with {{comments}} comments
      </p>
      <p class='desc'>{{desc}}</p>
    </li>
{{/issues}}
</ol>
</body>
</html>
