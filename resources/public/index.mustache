<!DOCTYPE html>
<body>
<h1>{{github-user}}'s Github Waiting Room</h1>
<ol>
{{#issues}}
    <li id="issue_{{position}}">
      <a href="{{url}}">{{name}}</a>
       {{type}} created by <a href="https://github.com/{{user}}">{{user}}</a>
       on {{created}}
       ({{comments}} comments)
      <p class='title'>{{title}}</p>
      <p class='desc'>{{desc}}</p>
    </li>
{{/issues}}
</ol>
</body>
</html>
